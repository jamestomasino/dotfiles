#!/usr/bin/env bash

if [ $# -ne 0 ]; then
  text="$*"
else
  text="sample"
fi

fonts=$(find "$(figlet -I2)" -maxdepth 1 -printf "%f\n" | grep "\.flf$" | cut -f 1 -d "." | sort -u)

for f in $fonts; do
  figlet -c -w 66 -f "$f" "$text"
done
